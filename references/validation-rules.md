# 定义成果机器闭环与按需验收规则

正式源说明：本文件是 CHARLS、ELSA 及后续数据库共用的唯一验收规则。独立验收不是定义流水线的固定工位，不得因任务类型、改动类型或历史习惯自动启动。

## 1. 默认路径：执行任务机器闭环

定义执行任务同时负责生成与验证。完成以下机器闸门后，直接标记 `MACHINE_CHECK_PASS / COMPLETE`，更新索引/台账并回传大脑：

1. 正式 runner 成功，唯一最终日志含明确退出码 0。
2. 固定检查器使用正确数据库参数运行并为 `ok: true`。
3. 正式目录、raw/header/codebook、analysis、mapping、禁止变量、唯一日志和用户材料结构检查通过。
4. 按实际改动完成足够的回归或独立复算，能够证明变量值、缺失、覆盖、来源身份和输出结构符合当前裁决。
5. 新增或修改公共机制时，相关 fixture、专项扫描和既有主题回归通过。
6. 过程证据存在、非空、可读；正式目录不混入过程材料。

机器闸门任一失败时，执行任务必须自行修正或向大脑报告阻断。不得把未闭环成果转交验收任务代为排错。

## 2. 独立验收仅有四个入口

只有出现以下情况之一，才启动独立只读验收：

1. 机器证据缺失、失败、互相矛盾，或出现执行任务无法解释的差异。
2. 存在当前规则和用户裁决无法解决的研究判断，例如来源语义、对象边界、计分口径或多来源 mismatch 仍有歧义。
3. 公共核心机制发生高影响修改，且现有 fixture、回归和机器复算不足以证明影响边界。
4. 用户或大脑明确要求独立验收。

新数据库、新主题、raw/transaction、变量集合、命名、顺序、schema、编码、mapping 或公共工具发生变化，本身都不是自动验收理由。只要口径已经裁决，机器证据完整且结果可解释，仍按默认机器闭环完成。

取消常规质量抽查，不按任务数量随机启动验收。

## 3. 独立验收的职责边界

独立验收只回答触发它的那个未决问题，不重复读取和核验执行任务已经机器证明的全部成果。

- 机器证据冲突：只复核冲突字段和对应生成链。
- 研究判断未决：只核证据边界并回大脑裁决，不替用户决定口径。
- 公共核心机制风险：只核受影响机制及代表性回归主题。
- 用户指定：严格按用户指定范围执行。

独立验收不得重新跑完整生产流程，不得把文件清单、日志、目录、普通文本同步和常规 xlsx 读取重新做一遍。

## 4. 状态与入账

- 默认路径：`GENERATED -> MACHINE_CHECK_PASS -> COMPLETE -> INDEXED`。
- 异常路径：`GENERATED -> MACHINE_CHECK_PASS/BLOCKED -> SENT_FOR_VALIDATION -> VALIDATION_PASS -> COMPLETE -> INDEXED`。
- 过程卡只需记录机器报告路径和是否存在上述四类异常；无异常时写 `independent_validation_required=false`。

独立验收任务保留为异常调查工具，不参与常规定义闭环。
